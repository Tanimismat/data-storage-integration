const add = (first, second) => {
    return first + second;
}

const multiply = (first, second) => {
    return first * second;
}

// export default add;
// if exporting more than one then have to pass as object shorthand

export { add, multiply }

